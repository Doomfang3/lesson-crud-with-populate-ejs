<%#!-- posts/list.hbs --%>

<h2>Posts</h2>

<ul>
  <% posts.forEach(({ title, author, _id }) => { %>
  <li>
    <%= title %> by <%= author.username %>
    <a href="/posts/<%= _id %>">Read more</a>
  </li>
  <% }) %>
</ul>
